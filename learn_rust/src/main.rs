fn main() {
    println!("Hello, world!");
}

// The main advantage of "cargo check" command is that it checks that
// the code can be compiled without compiling the code so that we can
// know that at that stage the code is correct
// It is much faster than the cmd cargo compile so run this instead for checking.
